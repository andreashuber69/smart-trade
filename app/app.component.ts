import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'my-app',
    template: "<my-dashboard></my-dashboard>"
})
export class AppComponent {
    title = 'Whatever';
}